"use client";
import Experience from '../Experience';

export default function Interactive3DTimeline() {
  // For now, always fallback to original Experience component
  // This ensures the build works while we can enhance later
  return <Experience />;
}