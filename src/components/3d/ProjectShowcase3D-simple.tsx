"use client";
import Projects from '../Projects';

export default function ProjectShowcase3D() {
  // For now, always fallback to original Projects component
  // This ensures the build works while we can enhance later
  return <Projects />;
}