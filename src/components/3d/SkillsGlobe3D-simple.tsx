"use client";
import Skills from '../Skills';

export default function SkillsGlobe3D() {
  // For now, always fallback to original Skills component
  // This ensures the build works while we can enhance later
  return <Skills />;
}